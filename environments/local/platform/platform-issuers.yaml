apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-issuers
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/leandrocarpenter/platform-gitops-engine.git
    targetRevision: main
    path: environments/local/platform/issuers
  destination:
    server: https://kubernetes.default.svc
    namespace: cert-manager
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true