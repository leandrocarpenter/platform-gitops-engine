global:
  domain: argocd.localdev.me

configs:
  params:
    server.insecure: true

server:
  replicas: 1
  ingress:
    enabled: true
    ingressClassName: traefik
    hostname: argocd.localdev.me
    path: /
    pathType: Prefix

dex:
  enabled: false

notifications:
  enabled: false

applicationSet:
  replicas: 1